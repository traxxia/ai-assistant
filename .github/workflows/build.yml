name: CI Build

on:
  push:
    branches:
      - qa
  pull_request:
    branches:
      - qa

env:
  IMAGE_NAME: traxxia-qa-ai-assistant

jobs:
  build-and-push:
    name: Build & Push to ACR
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'

      - name: Install dependencies
        run: npm install

      - name: Build Mastra project
        run: npm run build
        env:
          NODE_ENV: production

      - name: Log in to Azure Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.QA_ACR_LOGIN_SERVER }}
          username: ${{ secrets.QA_ACR_USERNAME }}
          password: ${{ secrets.QA_ACR_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: ${{ github.event_name == 'push' }}
          tags: |
            ${{ secrets.QA_ACR_LOGIN_SERVER }}/${{ env.IMAGE_NAME }}:latest
            ${{ secrets.QA_ACR_LOGIN_SERVER }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
